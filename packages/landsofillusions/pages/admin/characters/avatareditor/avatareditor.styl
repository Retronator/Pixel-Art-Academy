@import "{retronator:landsofillusions}/style/style.import"

.landsofillusions-pages-admin-characters-avatareditor
  adventure()
  pixel-image-rendering()

  *
  *::before
  *::after
    background-size 100% 100%
