@import "style.import"

html
  cursor default

a
button
  cursor pointer

input:not([type])
input[type='']
input[type=text]
input[type=email]
input[type=password]
input[type=number]
textarea
  cursor text
