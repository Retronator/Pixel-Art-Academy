@import "{retronator:landsofillusions}/style/style.import"

.landsofillusions-components-account-characters
  .load-character
    visibility hidden

    &.show-load-button
      visibility visible

  .current-character-editor
    .color
      margin-left 3em

      .dialog-preview
        display inline-block
        padding 1em
        background atari2600colors[1][0]
        pixel-text-outline black
