@import "{retronator:landsofillusions}/style/style.import"

.pixelartacademy-pixelboy-apps-yearbook.app
  adventure()
  color atariGray(0)

  .page-button
    position absolute
    display none
    border 0
    background 0
    padding 5rem
    checkout()
    color atariGray(4)
    cursor pointer

    &:hover
      color atariGray(3)

  &:hover
    .page-button.visible
      display block
