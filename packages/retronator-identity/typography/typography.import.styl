// Pixel art font smoothing

pixel-font-smoothing()
  -webkit-font-smoothing none
  -moz-osx-font-smoothing grayscale

// Font mixins

retrotype()
  font-family "Retrotype Retronator"
  font-weight normal
  font-size 12rem
  pixel-font-smoothing()

adventure()
  font-family "Adventure Retronator"
  font-weight normal
  font-size 6rem
  pixel-font-smoothing()

freehand()
  font-family "Freehand Retronator"
  font-weight normal
  font-size 9rem
  pixel-font-smoothing()

checkout()
  font-family "Checkout Retronator"
  font-weight normal
  font-size 6rem
  line-height 7rem
  pixel-font-smoothing()

academy()
  font-family "Academy Retronator"
  font-weight normal
  font-size 11rem
  line-height 12rem
  pixel-font-smoothing()

daily()
  font-family "Daily Retronator"
  font-weight normal
  font-size 7rem
  pixel-font-smoothing()

small-print()
  font-family "Small Print Retronator"
  font-weight normal
  font-size 5rem
  line-height 7rem
  pixel-font-smoothing()

illegible()
  font-family "Illegible Retronator"
  font-weight normal
  font-size 4rem
  line-height 5rem
  pixel-font-smoothing()

lines()
  font-family "Lines Retronator"
  font-weight normal
  font-size 2rem
  line-height 3rem
  pixel-font-smoothing()

acme-5()
  font-family "Acme 5 Retronator"
  font-size 8rem
  pixel-font-smoothing()

acme-7()
  font-family "Acme 7 Retronator"
  font-size 8rem
  pixel-font-smoothing()

acme-9()
  font-family "Acme 9 Retronator"
  font-size 8rem
  pixel-font-smoothing()

kyrou-5()
  font-family "Kyrou 5 Retronator"
  font-size 8rem
  pixel-font-smoothing()

kyrou-7()
  font-family "Kyrou 7 Retronator"
  font-size 8rem
  pixel-font-smoothing()

kyrou-9()
  font-family "Kyrou 9 Retronator"
  font-size 8rem
  pixel-font-smoothing()

uni()
  font-family "Uni Retronator"
  font-weight normal
  font-size 8rem
  pixel-font-smoothing()

zx-spectrum()
  font-family "ZX Spectrum Retronator"
  font-weight normal
  font-size 8rem
  pixel-font-smoothing()

thintel()
  font-family 'Thintel Retronator'
  font-weight normal
  font-size 16rem
  pixel-font-smoothing()

typecast()
  font-family 'Typecast Retronator'
  font-weight normal
  font-size 16rem
  pixel-font-smoothing()

chicago-mini()
  font-family 'Chicago Mini Retronator'
  font-weight normal
  font-size 9rem
  line-height 9rem
  pixel-font-smoothing()

geneva-mini()
  font-family 'Geneva Mini Retronator'
  font-weight normal
  font-size 7rem
  line-height 9rem
  pixel-font-smoothing()

serif-23()
  font-family 'Serif 23 Retronator'
  font-weight normal
  font-size 29rem
  pixel-font-smoothing()

pixel-georgia()
  font-family 'Pixel Georgia Retronator'
  font-weight normal
  font-size 10rem
  pixel-font-smoothing()

pixolde()
  font-family 'Pixolde Retronator'
  font-weight normal
  font-size 16rem
  pixel-font-smoothing()

talk-to-three-headed-monkey()
  font-family 'Talk-to-three-headed-monkey Retronator'
  font-weight normal
  font-size 12rem
  pixel-font-smoothing()
