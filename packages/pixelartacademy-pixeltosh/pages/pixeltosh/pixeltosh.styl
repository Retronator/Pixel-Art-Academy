@import "{retronator:retronator-identity}/style/style.import"

.pixelartacademy-pixeltosh-pages-pixeltosh
  pixel-image-rendering()
  user-select none
  adventure()
  color atariGray(7)

  *
  *::before
  *::after
    background-size 100% 100%

  .pixeltosh-safe-area
    position absolute
    top 13rem
    left 0
    width 320rem
    height 214rem
