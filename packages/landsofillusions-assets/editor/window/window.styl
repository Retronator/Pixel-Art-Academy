@import "{retronator:landsofillusions}/style/style.import"

.landsofillusions-assets-editor-window
  position fixed
  top 50%
  left 50%
  transform translate(-50%, -50%)
  border 1rem solid atariGray(2)
  background atariGray(1)
  box-shadow 5rem 5rem 0 rgba(atariGray(0), 0.5)

  .menu-bar
    padding 1rem
    background atariGray(0)
    border-bottom 1rem solid atariGray(2)

    .close-button
      position absolute
      top 1rem
      right 1rem
      z-index 1
      padding 0 1rem 0 3rem
      border none

      &::before
        content '×'

    .title
      text-align center
      text-transform uppercase
