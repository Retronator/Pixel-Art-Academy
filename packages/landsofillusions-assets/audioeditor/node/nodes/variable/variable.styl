@import "{retronator:retronator-identity}/style/style.import"

.landsofillusions-assets-audioeditor-node.artificial-echo-node-variable
  .parameter[data-name='id']
    select
      direction rtl

  .landsofillusions-assets-audioeditor-node-variable
    .parameters
      .parameter
        &.namespace
          select
            direction rtl

        input
        select
          width 74rem
