@import "{retronator:landsofillusions}/style/style.import"

.landsofillusions-assets-spriteeditor-pixelcanvas
  absolute-cover()
  user-select none
  background blend(rgba(atariGray(1), 0.5) atariGray(2))

  .canvas
    position absolute
    user-select none
