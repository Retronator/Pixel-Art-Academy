@import "{retronator:landsofillusions}/style/style.import"

.pixelartacademy-pixelboy-apps-journal-journalview-entry-object-image
  padding 3rem
  background atariGrey(7)
  box-shadow -2rem 1rem 0 transparentify(atariBrown(6), atariBrown(7))

  .image
    display block
    width 100%

  .missing-image
    margin 10rem
    text-align center
