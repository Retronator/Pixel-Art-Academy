@import "{retronator:retronator-identity}/style/style.import"
@import "{retronator:pixelartacademy-pixelpad-drawing}/editor/desktop/desktop.import"

.publication.sixty-four-squares
  position relative
  filter drop-shadow(-8rem 2rem 0 desktopShadowColor)
  transition filter 0.7s ease-out
  color atariGray(1)

  .pixelartacademy-pixelpad-apps-drawing-editor-desktop-publications.active &
    filter drop-shadow(-24rem 6rem 0 desktopShadowColor)

  &.cover
    .running-header
      display none

  &.cover
  &.table-of-contents
    .running-footer
      display none

  &.table-of-contents
  &.content-part
    &::before
      content ''
      position absolute
      inset -5rem -276rem 0 -6rem
      background-image image('/pixelartacademy/learnmode/pixelartfundamentals/fundamentals/publications/sixtyfoursquares/article.png')

    .contents-area.read-only
      right -100%
      mix-blend-mode multiply

  .contents-area
    position relative
    z-index 1

  .contents-area.read-only
    absolute-cover()
    overflow hidden

    .contents
      absolute-cover()
